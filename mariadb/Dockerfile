ARG MARIADB_VERSION
ARG MARIADB_USER
ARG MARIADB_PASSWORD
FROM bitnami/mariadb:${MARIADB_VERSION}

# Establecer las variables de entorno dentro del contenedor
ENV MARIADB_USER=${MARIADB_USER} \
    MARIADB_PASSWORD=${MARIADB_PASSWORD} \